using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PracticaOOP
{
    public class Rectangulo
    {
        /*
         * TODO: 8. Repita las actividades del 2 al 6 que
         * realizó para el Triángulo, adaptándolas a un Rectángulo
         * TIP: Recuerde que aunque un rectángulo tiene cuatro
         * lados, los lados paralelos son iguales, así que solo
         * necesita guardar los valores de sus lados contiguos (2 lados)
         */

        public double Area
        {
            get
            {
                double resultado = 0;

                /*
                 * TODO: 9. Calcule el área de un rectángulo. A diferencia del
                 * triángulo, en vez de usar un método utilizará una propiedad
                 * explícita (revise las diapositivas). Esta propiedad
                 * será de solo lectura y, por lo tanto, no necesita estar
                 * vinculada a algún atributo específico
                 * i.e. solo tendra la parte GET (no el SET)
                 * 
                 * TIP: Área de un rectángulo = a * b, donde a y b son la
                 *      longitud de los lados contiguos
                 */

                return resultado;
            }
        }
    }
}